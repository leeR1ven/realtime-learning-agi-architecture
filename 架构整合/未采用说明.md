# 本目录为未采用的整合草稿

2026-09-06，用户明确纠正：不需要分开保存记忆；模型共用时间神经元，所有记忆按顺序关联，不得擅自改架构。

本轮停止推进 `integrated_controller.py`、`recurrent_core.py` 与 `run_integration.py` 的整合方案。没有启动该方案的导航训练，也没有用它替换现有模型存档。已有单元实验结果仅用于追溯草稿，不能当作原架构的验收结果。

原始代码的时间机制见根目录 `海马体时间区.py`：全系统只有一个共享时钟，时间环每刻只有一个当前时间细胞激活。视觉、听觉、运动记忆均引用这个时钟。各区记录不同模态，不代表可以按事实、价值、任务或奖励状态另建时间体系。

后续检查应沿原有时间顺序和连接追踪：何时记录、哪些神经元共同激活、哪些连接兴奋或抑制、回忆如何推进到后续记忆及运动。奖励影响现有连接的强弱，不据此另建记忆类别。先给出具体错误的连接或调用证据，再做保持原架构的修复。

已发现但本次未修改的偏离：导航原型 `色标导航/route_switch_controller.py` 在目标活动改变时清空 `previous_event_id`，会切断连续经历的前驱。这并非此前同目标、只切换路线的失败原因；不能混为一谈。

本次纠正后只新增此状态说明，未继续修改模型算法。
