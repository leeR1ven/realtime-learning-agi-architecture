**保持8192个混合细胞不变，只丰富真实可见的墙面纹理后，训练合规到达从10/36提高到24/36，途中改道配对从0/6变成2/6。** 这表明当前配置中的环境可辨认性确实值得改善；结果仍不稳定，尚未证明完整前额叶递归思考或自然语言。

两次实验的出生连接哈希完全相同，36回合的起点、路线要求、顺序、随机种子及每回合1600帧上限相同。新条件只在实际射线命中的墙面上增加固定平滑颜色，纹理种子为44017；没有给模型坐标、地图、路线标签或方向答案。几何、碰撞、四肌肉、短音频率、奖励规则和第10秒干预协议保持不变。由于更早到达会结束回合，实际经历帧数不同，不能写成训练样本数量完全相等。

| 指标 | 灰墙 | 固定墙面纹理 |
| --- | --- | --- |
| 36回合合规奖励 | 10 | 24 |
| 下／上通道获奖回合 | 3／7 | 10／14 |
| 有效干预前缀 | 4/6 | 6/6 |
| 原路和改道均正确的配对 | 0/6 | 2/6 |
| 单看保持原提示分支合规 | 1/6 | 5/6 |
| 单看换提示分支合规 | 2/6 | 3/6 |
| 提示结束后清除规则活动的合规分支 | 0/6 | 0/6 |
| 真实经验事件 | 45,865 | 35,441 |
| 受奖事件 | 9,128 | 18,353 |
| 教师事件 | 0 | 0 |

两对通过的具体结果：

| 固定前缀 | 第10秒重复下通道提示 | 第10秒改成上通道提示 |
| --- | --- | --- |
| 种子831，初始下通道 | 经下通道，124.5秒到红色 | 经上通道，65.3秒到红色 |
| 种子1031，初始下通道 | 经下通道，58.7秒到红色 | 经上通道，157.6秒到红色 |

两分支都由完全相同的前100帧状态克隆，提示只持续0.6秒，首次通道均在提示结束后，测试全程不学习、不发奖励。因此这两对确实展示了新声音改变后续路线，并仍到达原目标。所有预先固定的其他种子与失败项均保留。

**方向仍不对称。** 三个“初始上通道、后来改下通道”的测试都仍走了上通道。训练覆盖也不完整：从左侧上部出发、要求下通道的6回合一次都没有获得成功经验；下通道的10次成功全部来自中部和下部，各5次。即使“上下两种规则都获得过奖励”，也不能据此认为模型在任意切换位置都已有可调用的完整经历。这个覆盖缺口和白盒检查发现的阶段混淆、当前递归通路限制需要继续分别处理。

保持原音、不再发音、反转对应后播放同一物理声音的控制保留为独立分支。后者改变的是环境评分期望，实际声音与保持分支相同；不能把分数不同说成行为不同。清除规则活动的对照也不能证明生物意义的前额叶必要性，它干预的是当前工程实现的规则保持群。

正式存档：[route_texture_evaluation.npz](F:/一种AGI架构/色标导航/results/route_texture_evaluation.npz)，SHA256：`c609e12caa78679d611bf87fb575eaea27b1ed66f8b437616261e2a6862a0715`。

完整协议、轨迹、克隆哈希和失败项：[route_texture_evaluation.json](F:/一种AGI架构/色标导航/results/route_texture_evaluation.json)。[独立终审](F:/一种AGI架构/色标导航/results/route_texture_independent_audit.json)已通过：30个分支克隆一致，逐项重新计算首／末通道与首次到达评分、冻结期间事件和强度不变、零奖励零教师及相同声音对照轨迹，均与正式结果一致。纹理物理验收：[textured_environment_audit.json](F:/一种AGI架构/色标导航/results/textured_environment_audit.json)。当前诊断：[白盒诊断.md](F:/一种AGI架构/色标导航/白盒诊断.md)、[实际失败连接追踪](F:/一种AGI架构/色标导航/results/route_failure_trace_1031_top_switch.md)、[固定感觉只换RGB的反事实检查](F:/一种AGI架构/色标导航/results/texture_encoding_counterfactual.md)。

双击 [启动纹理改道.cmd](F:/一种AGI架构/色标导航/启动纹理改道.cmd) 可打开实际在线环境。默认暂停；“新回合”才重置身体和短时活动，途中990/1320提示只改变PCM。生活经验另存`living_route_texture.npz`，正式存档只读。界面没有自动定时换路，日常运行的随机状态也不保证和上述冻结分支相同；使用正式脚本才能复现表中数字。

复现独立纹理条件：

```powershell
Set-Location -LiteralPath 'F:\一种AGI架构'
& '.\.venv\Scripts\python.exe' -X utf8 '色标导航\route_switch_experiment.py' --texture-seed 44017 --output '色标导航\results\route_texture_reproduction.json'
```

本次纹理分支最大的单回合控制p95为38.11毫秒，仍低于100毫秒步长。这不包含存档与全状态克隆，也不能外推任意模型规模。当前本机足够，不需要先租硬件来验证这两个已发现的问题。
